apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: collectorshop
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: dev
            namespace: collectorshop-dev
            path: deploy/environments/dev
            revision: develop
          - name: staging
            namespace: collectorshop-staging
            path: deploy/environments/staging
            revision: staging
          - name: prod
            namespace: collectorshop-prod
            path: deploy/environments/prod
            revision: main
  template:
    metadata:
      name: collectorshop-{{name}}
    spec:
      project: default
      source:
        repoURL: https://github.com/stanthbd/collectorshop.git
        targetRevision: "{{revision}}"
        path: "{{path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true

