apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - postgres.yaml
  - rabbitmq.yaml
  - catalog-service.yaml
  - moderation-worker.yaml
  - web-app.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/part-of: collectorshop
