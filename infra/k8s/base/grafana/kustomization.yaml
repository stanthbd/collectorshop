apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - resources.yaml

configMapGenerator:
  - name: grafana-dashboards
    files:
      - articles.json
  - name: grafana-dashboard-provisioning
    files:
      - provider.yaml=dashboards-provider.yaml
